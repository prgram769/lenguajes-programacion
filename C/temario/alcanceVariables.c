#include <stdio.h>

int main(void) {
    int edad = 37;
    /*La variable edad es una variable con alcance local*/
    printf("La variable edad, que en este caso su valor es %d, es una variable con alcance local ya que está dentro de una funcion.", edad);
}