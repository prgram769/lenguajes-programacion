#include <stdio.h>

int main(void) {
    int precios[5];

    printf("Asi, se declara un array.\n\n");
    printf("Asi, se asignan valores al array: \n\n");

    precios[0] = 1;
    precios[1] = 2;
    precios[2] = 3;
    precios[3] = 4;
    precios[4] = 5;
}