#include <stdio.h>

int main(void) {
    int a = 1;

    if (a = 0)
    {
        /*Esto nunca sera invocado porque el valor 0 siempre es falso y la sentencia solo se ejecuta si el valor resultante es verdadero*/
    }
    
}